# 01: Scaffold LLM-Wiki Knowledge Base & Project Core Structure

**What to build:**
Bootstrap the complete directory structure following the LLM-Wiki standard (`raw/articles/`, `raw/papers/`, `raw/notes/`, `raw/refs/`, `wiki/concepts/`, `wiki/entities/`, `wiki/summaries/`, `audit/resolved/`, `log/`, `outputs/queries/`, `src/`, `data/`, `web/`, `tests/`). Initialize `CLAUDE.md` with Thai political domain ontology, schemas, and naming conventions. Create the initial `wiki/index.md` catalog, today's operation log (`log/20260829.md`), and python environment dependencies.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] Directory tree created adhering to LLM-Wiki specification
- [x] `CLAUDE.md` populated with Thai politics domain schema, entity types, relationship types, and conventions
- [x] `wiki/index.md` initialized with navigation, categories, and placeholders
- [x] Daily log `log/20260829.md` created with initial scaffold entry
- [x] `requirements.txt` defined with necessary dependencies (requests, beautifulsoup4, feedparser, networkx, pydantic, pytest)
